{
    "Settings": {
        "Database": {
            "ConnectionString": "${Settings__Database__ConnectionString}",
            "DatabaseName": "${Settings__Database__DatabaseName}"
        }
    }
}